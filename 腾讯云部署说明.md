# 婚礼电子请柬：腾讯云部署说明

这份说明针对当前服务器：Ubuntu Server 22.04、Docker 26、Docker Compose v2.27.1。

## 先说结论

- 现有服务正在使用 3000、4173、8787 和本机端口 7700。
- 婚礼请柬使用 `127.0.0.1:3100`，不会占用或替换上述端口。
- 请柬的数据保存在项目目录的 `data/wedding.sqlite`，更新容器不会自动删除回执。
- 对公网只开放 80、443，不要在腾讯云安全组开放 3100。
- 当前服务器约有 14GB 磁盘空间，足够运行请柬。
- 当前服务器只有约 2GB 内存且没有 Swap。运行请柬没问题，但第一次在服务器构建镜像时可能内存紧张。

## 你的实际情况：北京服务器、暂无域名

北京属于中国大陆地域。按照腾讯云当前备案指引，使用中国大陆服务器开办网站，需要先完成 ICP 备案，取得备案号后再开通公网访问。

最稳妥的顺序是：

1. 在腾讯云购买域名；
2. 完成域名实名认证；
3. 实名认证完成并满足腾讯云规定的等待时间后，提交首次 ICP 备案；
4. 等待备案审核期间，可以把请柬装到服务器，但只通过服务器本机或 SSH 隧道验收；
5. 备案通过后，将域名解析到服务器，配置 Nginx、HTTPS 和微信分享测试；
6. 最后才把正式网址发给宾客。

不要为了测试在安全组中临时向公网开放 3100，也不要在备案完成前把公网 IP 当作正式请柬地址群发。

## 域名是什么

域名是宾客在微信里点开的地址，例如 `invite.example.com`。公网 IP 也能临时测试，但微信分享卡片、HTTPS 和正式使用都更适合域名。

这台服务器位于北京，域名正式解析到服务器并对外提供网页前，需要完成 ICP 备案。

你现在需要先完成两件事：

1. 在腾讯云域名注册页面搜索并购买一个未被注册的域名；个人婚礼请柬可优先考虑简短的 `.com` 域名。
2. 域名购买后使用与备案主体一致的真实身份资料完成实名认证。

## 本地项目里已经准备好的内容

- Docker 构建文件：`Dockerfile`
- Docker 启动配置：`compose.yaml`
- 环境变量模板：`.env.example`
- 回执数据库：首次启动时自动创建
- 管理入口：`/manage/rsvp`
- 回执 TXT 导出：在管理页面点击导出

## 第一步：把项目传到服务器

正式操作时，将整个 `wedding-invitation` 文件夹上传到服务器的：

```text
/home/ubuntu/wedding-invitation
```

不要上传这些本地生成目录：

```text
node_modules
.next
data
.env
```

可以使用腾讯云 OrcaTerm 的文件上传功能。进入 `/home/ubuntu` 后，上传完整项目文件夹即可。

## 第二步：创建正式配置

登录服务器后执行：

```bash
cd /home/ubuntu/wedding-invitation
cp .env.example .env
openssl rand -base64 36
openssl rand -base64 36
nano .env
```

前两次 `openssl` 会生成两段随机字符串。将 `.env` 改成类似下面这样：

```dotenv
ADMIN_PASSWORD=这里填写你自己设置的不少于12位的管理密码
ADMIN_SESSION_SECRET=粘贴第一段随机字符串
RATE_LIMIT_SECRET=粘贴第二段随机字符串
DATABASE_PATH=/app/data/wedding.sqlite
SITE_URL=https://你的正式域名
```

备案等待期间只进行服务器内部测试时，可暂填：

```dotenv
SITE_URL=http://127.0.0.1:3100
```

在 nano 里按 `Ctrl+O` 保存，回车确认，再按 `Ctrl+X` 退出。

注意：`.env` 里是真实密码，不要截图发到群里，也不要上传到代码仓库。

## 第三步：创建数据目录并构建

```bash
cd /home/ubuntu/wedding-invitation
mkdir -p data
sudo chown -R 1000:1000 data
sudo docker compose build
```

如果构建成功，再启动：

```bash
sudo docker compose up -d
sudo docker compose ps
curl -I http://127.0.0.1:3100
```

看到 `HTTP/1.1 200` 或重定向响应，说明请柬程序已正常运行。这个检查只发生在服务器内部，不代表网站已经向公网开放。

如果构建过程中出现 `Killed` 或内存不足，不要反复重试。保留报错信息，由我帮助选择下面一种方案：

- 在本地构建镜像后再上传；或
- 经你确认后，给服务器增加 1GB Swap。

## 第四步：备案通过后配置公网访问

请柬容器只监听 `127.0.0.1:3100`，外部无法直接访问，这是为了避免暴露额外端口。正式访问需要在服务器上使用 Nginx 或 Caddy，把 80/443 的请求转发到 3100。

由于服务器已有其他 Node 项目，配置反向代理前必须先确认那些项目目前通过什么网址访问，不能直接覆盖现有的 Nginx/Caddy 配置。

备案通过后，建议使用独立子域名，例如：

```text
invite.你的域名.com
```

它只转发到：

```text
http://127.0.0.1:3100
```

腾讯云安全组只需要保留或开放：

- TCP 22：SSH / OrcaTerm
- TCP 80：HTTP
- TCP 443：HTTPS

不要开放 3000、3100、4173、7700、8787 给所有公网来源。现有已开放端口是否可以收紧，需要先确认其它项目的使用方式后再处理。

## 管理回执

正式地址部署后，管理员入口为：

```text
https://你的域名/manage/rsvp
```

使用 `.env` 中的 `ADMIN_PASSWORD` 登录。管理页面可以：

- 查看已回复人数和总出席人数；
- 查看姓名、最新人数、填写时间和修改次数；
- 删除明显的恶意记录；
- 导出 UTF-8 编码的 TXT 名单。

同一个姓名重复填写时，以最新一次为准。例如先填 1 人、后来填 3 人，最终统计 3 人，不会相加成 4 人。历史修改会保留在数据库中。

群发链接无法百分之百阻止有人冒用他人姓名。当前版本通过频率限制、隐藏诱饵字段、数据校验和后台删除降低乱填风险。若以后需要更严格控制，可以再增加邀请码。

## 更新网站前先备份回执

```bash
cd /home/ubuntu/wedding-invitation
cp data/wedding.sqlite "data/wedding-$(date +%Y%m%d-%H%M%S).sqlite"
```

上传新版代码后：

```bash
sudo docker compose build
sudo docker compose up -d
sudo docker compose ps
```

## 查看运行状态和日志

```bash
cd /home/ubuntu/wedding-invitation
sudo docker compose ps
sudo docker compose logs --tail=100 wedding-invitation
```

停止请柬但保留数据：

```bash
sudo docker compose down
```

重新启动：

```bash
sudo docker compose up -d
```

不要执行带 `-v` 的 `docker compose down -v`，也不要删除 `data` 文件夹，否则可能丢失回执数据。

## 微信分享卡片

网站已经准备了标题、简介和 500×500 分享图。微信中能否稳定显示为带缩略图的分享卡片，还取决于：

- 使用可公网访问的正式 HTTPS 域名；
- 微信能够抓取分享图；
- 如果要完全自定义微信分享标题、简介和图片，通常还需要已认证的微信公众号以及微信 JS-SDK 配置。

部署完成后，再用真实微信环境进行最终测试。
